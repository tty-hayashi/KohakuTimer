define("amber-timer/Timer-Tests", ["amber/boot", "amber_core/SUnit"], function($boot){
var $core=$boot.api,nil=$boot.nil,$recv=$boot.asReceiver,$globals=$boot.globals;
var smalltalk=$core,_st=$recv,globals=$globals;
$core.addPackage('Timer-Tests');
$core.packages["Timer-Tests"].transport = {"type":"amd","amdNamespace":"amber-timer"};

$core.addClass('TimerTest', $globals.TestCase, [], 'Timer-Tests');

});
